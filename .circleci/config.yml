jobs:
  build:
    docker:
      - image: circleci/golang
    working_directory: /tmp/project
    steps:
      - attach_workspace:
          at: /tmp/project/serverr
      - run: go build server.go
      - store_artifacts:
          path: /code

  test:
    docker:
      - image: circleci/golang
    working_directory: /tmp/project
    steps:
      - checkout
      - run: go test
      - persist_to_workspace:
          root: /tmp/workspace
          paths: 
            - server
      - run: ls server

workflows:
  version: 2
  test-build:
    jobs:
      - test
      - build:
          requires:
            - test