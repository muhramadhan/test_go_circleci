jobs:
  build:
    docker:
      - image: circleci/golang
    steps:
      - attach_workspace:
          at: /tmp/workspace
      - run: go build server.go
      - store_artifacts:
          path: /code

  test:
    docker:
      - image: circleci/golang
    steps:
      - checkout
      - run: mkdir -p tmp/workspace
      - run: go test
      - persist_to_workspace:
          root: tmp/workspace
          paths: server

workflows:
  version: 2
  test-build:
    jobs:
      - test
      - build:
          requires:
            - test